#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp (ql:quickload '(:parenscript
                              :ps-experiment
                              :ps-experiment-test
                              :prove
                              :uiop)
                            :silent t))

(defpackage :ros.script.success.3724219295
  (:use :cl
        :prove
        :parenscript
        :ps-experiment
        :ps-experiment-test.test-utils))
(in-package :ros.script.success.3724219295)

(defun.ps+ success-func1 () 0)
(defun.ps+ success-func2 () 1)

(defun main (&rest argv)
  (declare (ignorable argv))
  (plan 1)
  (subtest "group"
    (with-prove-in-both ()
      (is (success-func1) 0)
      (is (success-func2) 1)))
  (unless (finalize)
    (uiop:quit -1)))
;;; vim: set ft=lisp lisp:
